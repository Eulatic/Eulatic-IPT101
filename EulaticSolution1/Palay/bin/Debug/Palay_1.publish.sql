/*
Deployment script for Palay

This code was generated by a tool.
Changes to this file may cause incorrect behavior and will be lost if
the code is regenerated.
*/

GO
SET ANSI_NULLS, ANSI_PADDING, ANSI_WARNINGS, ARITHABORT, CONCAT_NULL_YIELDS_NULL, QUOTED_IDENTIFIER ON;

SET NUMERIC_ROUNDABORT OFF;


GO
:setvar DatabaseName "Palay"
:setvar DefaultFilePrefix "Palay"
:setvar DefaultDataPath "C:\Users\Admin\AppData\Local\Microsoft\Microsoft SQL Server Local DB\Instances\MSSQLLocalDB\"
:setvar DefaultLogPath "C:\Users\Admin\AppData\Local\Microsoft\Microsoft SQL Server Local DB\Instances\MSSQLLocalDB\"

GO
:on error exit
GO
/*
Detect SQLCMD mode and disable script execution if SQLCMD mode is not supported.
To re-enable the script after enabling SQLCMD mode, execute the following:
SET NOEXEC OFF; 
*/
:setvar __IsSqlCmdEnabled "True"
GO
IF N'$(__IsSqlCmdEnabled)' NOT LIKE N'True'
    BEGIN
        PRINT N'SQLCMD mode must be enabled to successfully execute this script.';
        SET NOEXEC ON;
    END


GO
USE [$(DatabaseName)];


GO
/*
The column [dbo].[palay].[Palay_id] is being dropped, data loss could occur.

The column [dbo].[palay].[PalayId] on table [dbo].[palay] must be added, but the column has no default value and does not allow NULL values. If the table contains data, the ALTER script will not work. To avoid this issue you must either: add a default value to the column, mark it as allowing NULL values, or enable the generation of smart-defaults as a deployment option.
*/

IF EXISTS (select top 1 1 from [dbo].[palay])
    RAISERROR (N'Rows were detected. The schema update is terminating because data loss might occur.', 16, 127) WITH NOWAIT

GO
PRINT N'Starting rebuilding table [dbo].[palay]...';


GO
BEGIN TRANSACTION;

SET TRANSACTION ISOLATION LEVEL SERIALIZABLE;

SET XACT_ABORT ON;

CREATE TABLE [dbo].[tmp_ms_xx_palay] (
    [PalayId]         INT             NOT NULL,
    [Variety]         VARCHAR (100)   NOT NULL,
    [Quantity]        DECIMAL (10, 2) NOT NULL,
    [WeightUnit]      VARCHAR (10)    NOT NULL,
    [MoistureContent] DECIMAL (5, 2)  NULL,
    [PricePerUnit]    DECIMAL (10, 2) NOT NULL,
    [HarvestDate]     DATE            NULL,
    [FarmerName]      VARCHAR (255)   NULL,
    PRIMARY KEY CLUSTERED ([PalayId] ASC)
);

IF EXISTS (SELECT TOP 1 1 
           FROM   [dbo].[palay])
    BEGIN
        INSERT INTO [dbo].[tmp_ms_xx_palay] ([Variety], [Quantity], [WeightUnit], [MoistureContent], [PricePerUnit], [HarvestDate], [FarmerName])
        SELECT [Variety],
               [Quantity],
               [WeightUnit],
               [MoistureContent],
               [PricePerUnit],
               [HarvestDate],
               [FarmerName]
        FROM   [dbo].[palay];
    END

DROP TABLE [dbo].[palay];

EXECUTE sp_rename N'[dbo].[tmp_ms_xx_palay]', N'palay';

COMMIT TRANSACTION;

SET TRANSACTION ISOLATION LEVEL READ COMMITTED;


GO
PRINT N'Creating Procedure [dbo].[CreatePalay]...';


GO
CREATE PROCEDURE [dbo].[CreatePalay]
	@PalayId INT,
    @Variety VARCHAR(100) = NULL,
    @Quantity DECIMAL(10, 2) = NULL,
    @WeightUnit VARCHAR(10) = NULL,
    @MoistureContent DECIMAL(5, 2) = NULL,
    @PricePerUnit DECIMAL(10, 2) = NULL,
    @HarvestDate DATE = NULL,
    @FarmerName VARCHAR(255) = NULL
AS
BEGIN
     INSERT INTO [dbo].[palay] ([PalayId], [Variety], [Quantity], [WeightUnit], [MoistureContent], [PricePerUnit], [HarvestDate], [FarmerName])
     VALUES (@PalayId, @Variety, @Quantity, @WeightUnit, @MoistureContent, @PricePerUnit, @HarvestDate, @FarmerName)
END
GO
PRINT N'Creating Procedure [dbo].[DeletePalay]...';


GO
CREATE PROCEDURE [dbo].[DeletePalay]
	@param1 int = 0,
	@param2 int
AS
	SELECT @param1, @param2
RETURN 0
GO
PRINT N'Creating Procedure [dbo].[SelectPalay]...';


GO
CREATE PROCEDURE [dbo].[SelectPalay]
	@param1 int = 0,
	@param2 int
AS
	SELECT @param1, @param2
RETURN 0
GO
PRINT N'Creating Procedure [dbo].[UpdatePalay]...';


GO
CREATE PROCEDURE [dbo].[UpdatePalay]
	@param1 int = 0,
	@param2 int
AS
	SELECT @param1, @param2
RETURN 0
GO
PRINT N'Update complete.';


GO
